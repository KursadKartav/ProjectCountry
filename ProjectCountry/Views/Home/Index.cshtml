@model ProjectCountry.Models.WorldModel

<h1>CountryList</h1>


    <div class="row">
        <div class="col-md-4">
            <form asp-action="CountryList">
                @if (TempData["Country"] != null)
                {
                    <div class="form-group">
                        <label asp-for="Countries" class="control-label"></label>
                        <select asp-for="CountryId" asp-items="ViewBag.Count" class="form-control-sm">
                            <option value="">--Select Country--</option>
                            <option value="">@TempData["Country"]</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label asp-for="States" class="control-label"></label>
                        <select asp-for="StateId" asp-items="ViewBag.State" class="form-control-sm">
                            <option value="">--Select State--</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Find" class="btn btn-primary" />
                    </div>
                    <div>
                        @Html.ActionLink("Listeye Geri Dön", "Index")
                    </div>
                }
                else
                {
                    <div class="form-group">
                        <label asp-for="Countries" class="control-label"></label>
                        <select asp-for="CountryId" asp-items="ViewBag.Count" class="form-control-sm">
                            <option value="">--Select Country--</option>
                        </select>

                    </div>
                    @*<div class="form-group">
                        <label asp-for="States" class="control-label"></label>
                        <select asp-for="StateId" asp-items="ViewBag.State" class="form-control-sm">
                            <option value="">--Select State--</option>
                        </select>
                    </div>*@
                    <div class="form-group">
                        <input type="submit" value="Find" class="btn btn-primary" />
                    </div>
                }
            </form>
        </div>
    </div>
